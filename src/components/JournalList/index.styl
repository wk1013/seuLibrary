.book_listItem
  padding: 20px;
  background: #fff;
  .top
    font-size: 28px;
  .bottom
    font-size: 24px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    .b-left
      .img
        width: 160px;
        height: auto;
    